<% content_for :head do %>
  <meta name="title" content="<%= @title %>">
  <meta name="description" content="Works 一覧">
<% end %>

<div class="container index-work">
    <div class="container">
      <%= link_to 'Signin with Google', user_omniauth_authorize_path(:google) %>

      <%= render 'works/list' , works: @works %>
    </div>
</div>
